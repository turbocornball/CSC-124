import random

def Rand(start, num, end):
    res = []
    
    for j in range(num):
        res.append(random.randint(start,end))
    
    return res



def insertionSort(Rand):
    for index in range(1,len(Rand)):

        currentvalue = Rand[index]
        position = index 

        while position > 0 and Rand[position-1] > currentvalue:
            Rand[position] = Rand[position-1]
            position = position-1
        
        Rand[position] = currentvalue

insertionSort(Rand)
print(Rand)
